<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" /> 
    <title>REJA</title>
    <link rel="stylesheet"
href="https://stackpath.bootstrapcdn.com/bootstrap/4.2.1/css/bootstrap.min.css" 
integrity="sha384-GJzZqFGwb1QTTN6wy59ffF1BuGJpLSa9DkKMp0DgiMDm4iYMj70gZWKYbI706tWS"
crossorigin="anonymous" />
</head>
<div style="background: linear-gradient(120deg, #b8bbd1, #eaeaf5);">
    <div class="container mt-5">
  <h1 class="display-4 text-center mb-4 text-dark">
     REJA VA MAQSADLAR
  </h1>

  <div class="card shadow-lg border-0">
    <div class="card-body p-4">
      <form id="create-form" action="/create-item" method="POST">
        <div class="form-group d-flex align-items-center">
          
          <input
            id="create-field"
            name="reja"
            type="text"
            class="form-control form-control-lg mr-3"
            placeholder="Yangi reja qo'shish..."
            autofocus
            autocomplete="off"
          />

          <button
            class="btn btn-primary btn-lg px-4"
            style="border-radius: 20px;">
             Yangi Reja Qo'shish 
          </button>
        </div>
      </form>
    </div>


<ul id="item-list" class="list-group pb-5">

  <% item.map(function(item) { %>
    <li class="list-group-item list-group-item-info d-flex align-items-center justify-content-between">

      <span class="item-text"><%= item.reja %></span>

      <div>
        <button data-id="<%= item._id %>" class="edit-me btn btn-warning btn-sm">
          O'zgartirish
        </button>

        <button data-id="<%= item._id %>" class="delete-me btn btn-danger btn-sm">
          O'chirish
        </button>
      </div>

    </li>
  <% }); %>
</ul>

<button id="clean-all" class="btn btn-danger" style = "border-radius:20px">
  Hammasini o'chirish
</button>
</div>

<script src="https://unpkg.com/axios/dist/axios.min.js"></script>
<script src="/browser.js"></script>
</body>
</html> 